package com.driftlessdays.app.ui.calendar

enum class PhotoMode {
    SAME_AS_MONTH,    // Same photo all month
    DATE_SPECIFIC,    // Different photo per date
    RANDOM            // Random photo each view
}